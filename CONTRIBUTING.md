# 新增與維護 Skill 規範

本文件定義此 repository 的收錄標準。目標是讓一位首次接觸此專案的人，或一個新的 AI agent，都能在不依賴口頭說明的情況下找到、理解、安裝並正確使用任一 skill。

## 新增 skill 的必要項目

1. 在根目錄建立唯一的 `<skill-slug>/` 資料夾；名稱使用全小寫英數與連字號。
2. 建立 `<skill-slug>/README.md`，以 [模板](templates/SKILL-README-template.md) 為起點，完成所有「必填」段落。
3. 若 skill 是給 AI 執行的一組指令，建立 `<skill-slug>/SKILL.md`；內容應明確、可操作，且不假設讀者知道作者的背景脈絡。
4. 在根目錄 `SKILLS_INDEX.md` 新增一列，提供一句話用途、主要情境與相對文件連結。
5. 一併提交必要的 scripts、references、assets 與可重現的範例；補上授權資訊（若有第三方素材）。

## README 的品質標準

每份 skill README 至少要回答下列問題：

- **目的：** 它替誰解決什麼問題？何時適合或不適合使用？
- **原理與特殊設計：** 它如何運作？有哪些重要流程、決策、資料格式、限制、保護措施或刻意的取捨？避免只列檔案名稱，應交代「為什麼這樣設計」。
- **使用方法：** 前置需求、安裝／放置位置、最小可用範例、輸入與預期輸出、驗證方法。
- **操作細節：** 參數、設定、檔案結構與常見工作流程（依 skill 複雜度選擇）。
- **限制與安全：** 已知限制、成本、權限、隱私、敏感資料與不可逆操作。
- **維護資訊：** 版本、最後更新日期、相容環境、變更紀錄或連結。

文件以繁體中文為主；命令、程式碼、檔案名稱與必要技術名詞維持原文。資訊不足時，標示 `待補`，不可假裝已驗證。

## 提交前檢查

- [ ] 目錄名稱與 `SKILLS_INDEX.md` 連結正確。
- [ ] README 的必填段落已完成，且新讀者能照著完成一次最小操作。
- [ ] 特殊設計、限制與安全考量已被說明。
- [ ] 指令、路徑、檔名和範例已實際檢查。
- [ ] 沒有憑證、token、個資、暫存檔或不必要的大型檔案。
- [ ] 若更新了既有 skill，README 的「最後更新」與變更紀錄已同步。
